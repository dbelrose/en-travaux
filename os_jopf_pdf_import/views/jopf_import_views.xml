<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue formulaire -->
    <record id="view_jopf_import_form" model="ir.ui.view">
        <field name="name">jopf.import.form</field>
        <field name="model">jopf.import</field>
        <field name="arch" type="xml">
            <form string="Import JOPF">
                <header>
                    <button name="action_import"
                            string="Importer"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_reset_to_draft"
                            string="Remettre en brouillon"
                            type="object"
                            invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_partners"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-users"
                                invisible="not partner_ids">
                            <field name="person_count" widget="statinfo" string="Personnes"/>
                        </button>
                    </div>

                    <group>
                        <group>
                            <field name="name"/>
                            <field name="file_data" filename="file_name"/>
                            <field name="file_name" invisible="1"/>
                        </group>
                        <group>
                            <field name="date_import"/>
                            <field name="date_jopf"/>
                            <field name="ocr_language"/>
                            <field name="ocr_dpi"/>
                            <field name="ocr_psm"/>
                        </group>
                    </group>

                    <group>
                        <group>
                            <field name="association_count"/>
                            <field name="person_count"/>
                        </group>
                        <group>
                            <field name="error_count"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Contenu extrait" invisible="not text_content">
                            <field name="text_content" readonly="1"/>
                        </page>

                        <page string="Partenaires" invisible="not partner_ids">
                            <field name="partner_ids" readonly="1">
                                <tree>
                                    <field name="name"/>
                                    <field name="is_company"/>
                                    <field name="parent_id"/>
                                    <field name="function"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Détails" invisible="not line_ids">
                            <field name="line_ids" readonly="1">
                                <tree>
                                    <field name="association_name"/>
                                    <field name="person_name"/>
                                    <field name="role"/>
                                    <field name="state" widget="badge"
                                           decoration-success="state == 'created'"
                                           decoration-info="state == 'updated'"
                                           decoration-danger="state == 'error'"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Log" invisible="not log">
                            <field name="log" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>

                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Vue liste -->
    <record id="view_jopf_import_tree" model="ir.ui.view">
        <field name="name">jopf.import.tree</field>
        <field name="model">jopf.import</field>
        <field name="arch" type="xml">
            <tree string="Imports JOPF">
                <field name="name"/>
                <field name="date_import"/>
                <field name="date_jopf"/>
                <field name="association_count"/>
                <field name="person_count"/>
                <field name="state"
                       widget="badge"
                       decoration-success="state == 'imported'"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'processing'"
                       decoration-danger="state == 'error'"/>
            </tree>
        </field>
    </record>

    <!-- Vue recherche -->
    <record id="view_jopf_import_search" model="ir.ui.view">
        <field name="name">jopf.import.search</field>
        <field name="model">jopf.import</field>
        <field name="arch" type="xml">
            <search string="Rechercher imports">
                <field name="name"/>
                <field name="date_jopf"/>
                <filter string="Brouillon" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Importés" name="imported" domain="[('state', '=', 'imported')]"/>
                <filter string="Erreur" name="error" domain="[('state', '=', 'error')]"/>
                <separator/>
                <filter string="Ce mois" name="this_month"
                        domain="[('date_import', '&gt;=', (context_today() - relativedelta(months=1)).strftime('%Y-%m-01'))]"/>
                <group expand="0" string="Grouper par">
                    <filter string="État" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Date import" name="group_date" context="{'group_by': 'date_import'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
