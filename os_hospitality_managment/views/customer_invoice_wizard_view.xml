<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue formulaire pour le wizard de configuration des factures clients -->
    <record id="view_customer_invoice_config_wizard_form" model="ir.ui.view">
        <field name="name">customer.invoice.config.wizard.form</field>
        <field name="model">customer.invoice.config.wizard</field>
        <field name="arch" type="xml">
            <form string="Configuration des factures clients">
                <sheet>
                    <div class="oe_title">
                        <h1>Assistant de génération des factures clients</h1>
                        <p>Configurez les paramètres pour générer automatiquement les factures clients à partir des réservations.</p>
                    </div>
                    
                    <group>
                        <group string="Mois à traiter" name="month_selection">
                            <field name="booking_month_id" required="1" 
                                   domain="[('customer_invoices_generated', '=', False), ('total_revenue', '>', 0)]"
                                   options="{'no_create': True}"/>
                        </group>
                        <group string="Prévisualisation" name="preview" invisible="not booking_month_id">
                            <field name="preview_customer_count" readonly="1"/>
                            <field name="preview_total_amount" readonly="1" widget="monetary"/>
                            <field name="company_currency_id" invisible="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Configuration produits" name="products">
                            <group>
                                <group string="Produit Hébergement">
                                    <field name="accommodation_product_id" 
                                           options="{'no_create': True, 'no_open': True}"/>
                                    <field name="accommodation_price"/>
                                    <button name="action_create_products" 
                                            string="Créer/Mettre à jour les produits"
                                            type="object"
                                            class="btn-secondary"
                                            icon="fa-plus"/>
                                </group>
                                <group string="Produit Taxe de séjour">
                                    <field name="include_tax_on_invoice"/>
                                    <field name="tax_product_id" 
                                           invisible="not include_tax_on_invoice"
                                           options="{'no_create': True, 'no_open': True}"/>
                                    <field name="tax_price" invisible="not include_tax_on_invoice"/>
                                </group>
                            </group>
                        </page>

                        <page string="Options de facturation" name="options">
                            <group>
                                <group string="Regroupement">
                                    <field name="group_by_customer"/>
                                    <p class="text-muted" invisible="not group_by_customer">
                                        Toutes les réservations d'un même client sur le mois seront regroupées sur une seule facture.
                                    </p>
                                    <p class="text-muted" invisible="group_by_customer">
                                        Une facture sera créée pour chaque réservation individuelle.
                                    </p>
                                </group>
                                <group string="Validation">
                                    <field name="auto_validate_invoices"/>
                                    <p class="text-muted" invisible="not auto_validate_invoices">
                                        Les factures seront automatiquement validées après création.
                                    </p>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>

                <footer>
                    <button name="action_preview_invoices" 
                            string="Prévisualiser"
                            type="object" 
                            class="btn-secondary"
                            invisible="not booking_month_id"/>
                    <button name="action_generate_invoices" 
                            string="Générer les factures"
                            type="object" 
                            class="btn-primary"
                            invisible="not booking_month_id"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action pour le wizard de configuration des factures clients -->
    <record id="action_customer_invoice_config_wizard" model="ir.actions.act_window">
        <field name="name">Assistant factures clients</field>
        <field name="res_model">customer.invoice.config.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_customer_invoice_config_wizard_form"/>
    </record>

    <!-- Action contextuelle depuis booking.month -->
    <record id="action_customer_invoice_config_from_month" model="ir.actions.act_window">
        <field name="name">Configurer factures clients</field>
        <field name="res_model">customer.invoice.config.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_customer_invoice_config_wizard_form"/>
        <field name="context">{'default_booking_month_id': active_id}</field>
    </record>

    <!-- Ajout dans le menu -->
    <menuitem id="menu_customer_invoice_wizard"
              name="🧙 Assistant factures clients"
              parent="menu_hospitality_config"
              action="action_customer_invoice_config_wizard"
              sequence="15"/>

</odoo>